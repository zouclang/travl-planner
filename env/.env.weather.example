# Multi-source weather config (API or MCP gateway)
# Usage:
#   source ./.env.weather

# Query order (left -> right). Supported aliases:
# weathercn, moji, nmcc, amap, mcp, custom
export WEATHER_PROVIDER_ORDER="weathercn,moji,nmcc,amap"

# Generic custom weather endpoint (optional)
export WEATHER_QUERY_ENDPOINT=""

# Weather.com.cn endpoint (optional)
export WEATHERCN_QUERY_ENDPOINT=""

# Moji endpoint (optional)
export MOJI_QUERY_ENDPOINT=""

# NMCC endpoint (optional)
export NMCC_QUERY_ENDPOINT=""

# MCP-HTTP bridge endpoint (optional)
export WEATHER_MCP_ENDPOINT=""

# Amap fallback key (optional if not using amap in provider order)
export AMAP_WEB_API_KEY=""
